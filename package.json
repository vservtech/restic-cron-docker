{
  "name": "restic-cron-docker",
  "version": "1.5.0",
  "description": "This repo contains a dockerfile which allows running a restic cron job inside a docker container.",
  "private": true,
  "config": {
    "image_name": "vservtech/restic-cron-docker"
  },
  "scripts": {
    "dev": "docker compose up --build --remove-orphans",
    "start": "docker compose up -d --build --remove-orphans",
    "stop": "docker compose down",
    "shell": "docker compose run --build --remove-orphans restic-cron ash",
    "build": "docker build -t $npm_package_config_image_name:$(bunx project-version) -t $npm_package_config_image_name:latest .",
    "build-amd64": "docker buildx build --platform linux/amd64 --no-cache --pull --load -t $npm_package_config_image_name:$(bunx project-version) . --load",
    "docker-login": "docker login",
    "deployx": "docker buildx build --platform linux/amd64,linux/arm64 --no-cache --pull --push -t $npm_package_config_image_name:$(bunx project-version) -t $npm_package_config_image_name:latest .",
    "setup-buildx": "docker buildx create --name jb-buildx --use",
    "use-buildx": "docker buildx use jb-buildx",
    "_legacy": "",
    "push:version": "docker push $npm_package_config_image_name:$(bunx project-version)",
    "push:latest": "docker push $npm_package_config_image_name:latest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vserv-tech/restic-cron-docker.git"
  },
  "keywords": [
    "docker",
    "docker-compose",
    "restic"
  ],
  "author": "Benjamin Jesuiter <bjesuiter@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/vserv-tech/restic-cron-docker/issues"
  },
  "homepage": "https://github.com/vserv-tech/restic-cron-docker#readme",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "project-version": "2.0.0"
  }
}