{
  "name": "ansible-controller-docker",
  "version": "2.0.0",
  "description": "This repo contains a dockerfile which allows running an ansible controller inside a docker container.",
  "private": true,
  "config": {
    "image": "vservtech/ansible-controller-docker"
  },
  "scripts": {
    "start": "docker compose up -d --build",
    "stop": "docker compose down",
    "exec": "docker compose run ansible /bin/bash",
    "shell": "docker compose run ansible ash",
    "build": "docker build -t ${npm_package_config_image}:$(npx project-version) .",
    "tag:latest": "docker tag ${npm_package_config_image}:$(npx project-version) ${npm_package_config_image}:latest",
    "test": "docker-compose -f docker.test.yml up",
    "deploy": "run-s tag:latest push:version push:latest",
    "push:version": "docker push ${npm_package_config_image}:$(npx project-version)",
    "push:latest": "docker push ${npm_package_config_image}:latest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vserv-tech/ansible-controller-docker.git"
  },
  "keywords": [
    "docker",
    "docker-compose",
    "ansible"
  ],
  "author": "Benjamin Jesuiter <bjesuiter@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/vserv-tech/ansible-controller-docker/issues"
  },
  "homepage": "https://github.com/vserv-tech/ansible-controller-docker#readme",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "project-version": "^1.2.0"
  }
}
